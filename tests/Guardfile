guard 'sass',
	:load_paths => ['../assets/stylesheets'],
  # :debug_info => true,
  :input => 'scss',
  :output => 'css'

guard 'livereload' do
  # watch(%r{../assets/stylesheets/.+\.(scss)})
  watch(%r{grid/.+\.(html)})
  watch(%r{scss/.+\.(scss)})
end
